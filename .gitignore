# =====================================
# Python
# =====================================
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtualenvs
.venv/
venv/
ENV/
env/
env.bak/
pythonenv*

# Byte-compiled / optimized / DLL files
*.dll
*.dylib
*.exe
*.pyd

# =====================================
# Django
# =====================================
*.log
*.pot
*.pyc
*.sqlite3
db.sqlite3
debug.log

# Media y archivos de usuario
/media/
/uploads/
/temp/
/tmp/
/*.bak

# Outputs de collectstatic (docker-compose mapea static/ como STATIC_ROOT).
# La fuente canónica de estos assets vive en apps/<app>/static/<app>/.
/static/georeferenciacion/

# Assets del /admin de Django, commiteados por error en una corrida de
# collectstatic. Los 125 archivos eran BYTE A BYTE idénticos a los que trae
# Django 4.2.11 (verificado 2026-08-06: 0 overrides, 0 extras), así que hoy no
# rompían nada. El problema es a futuro: `static/` está en STATICFILES_DIRS y
# FileSystemFinder corre ANTES que AppDirectoriesFinder, así que al actualizar
# Django estas copias viejas taparían las nuevas — y el /admin se rompería de
# una forma difícil de rastrear, porque el código estaría bien.
/static/admin/

# STATIC_ROOT (settings.py). Es output de collectstatic, se regenera en deploy;
# el runtime real usa el mount de static/ (docker-compose), no esta carpeta.
/staticfiles/

# Archivos de migración (opcional, si no quieres versionarlas)
**/migrations/*.py
!**/migrations/__init__.py

# =====================================
# Docker
# =====================================
.docker/
# .dockerignore NO se ignora: es lo único que impide que `COPY . .` hornee el
# .env, el .git y el .claude dentro de la imagen. Debe viajar con el repo.
docker-compose.override.yml
.env
.env.local
.env.*.local
docker-compose.yml
# Excepción: la copia de referencia en docs/infra/ (solo ${VAR}, sin secretos)
# se versiona como documentación de despliegue.
!docs/infra/artefactos/docker-compose.yml

# =====================================
# Node / Frontend
# =====================================
node_modules/
dist/
build/
.npm/
.yarn/
.pnp.*
package-lock.json
yarn.lock
# Etapa D Plan Frontend: el package-lock.json de frontend/ SÍ se commitea
# (build reproducible en CI). La regla genérica de arriba excluye otros.
!frontend/package-lock.json

# =====================================
# IDEs y herramientas
# =====================================
.vscode/
.idea/
*.iml

# =====================================
# Sistema
# =====================================
.DS_Store
Thumbs.db
desktop.ini

# =====================================
# Coverage / Testing
# =====================================
.coverage
.tox/
.nox/
.cache/
pytest_cache/
coverage.xml
htmlcov/

# =====================================
# Secretos y archivos locales de operación (NUNCA a git)
# =====================================
# Rosters de usuarios/contraseñas temporales que genera el admin.
# El propio archivo declara "GITIGNORED · NO subir a git".
*.local.txt
credenciales_*.txt
# Control de usuarios: nombre + cédula + contrato CPS de personas reales. El
# repo es PÚBLICO y eso es habeas data (Ley 1581). El control se mantiene: los
# archivos siguen existiendo en la máquina del admin, solo no se versionan.
# Es la misma regla que `crear_usuarios_georef.py` ya enuncia para las claves.
*.local.md

# =====================================
# Worktrees efímeros de agentes (Claude Code)
# =====================================
.claude/worktrees/

# =====================================
# Otros
# =====================================
*.swp
*.swo
*.orig

# =====================================
# Frontend Angular (Etapa D Plan Frontend)
# =====================================
frontend/node_modules/
frontend/dist/
frontend/.angular/
frontend/coverage/
frontend/.cache/
frontend/npm-debug.log
frontend/.env
frontend/tmp/
frontend/openapi.yml
credenciales_georef.local.txt

# Deps webpack pre-Angular (basura, reinstalable)
static/node_modules/

# ── Exportes con datos personales (2026-08-05) ───────────────────────
# La raíz del repo tenía tres CSV con 4.582 nombres completos + cédulas,
# versionados desde abril en un repositorio PÚBLICO. Se verificó que las
# 4.382 personas y sus documentos estaban en `poblacion_kennedy` antes de
# borrarlos: el CSV no era la fuente de nada.
#
# Ojo: esto impide que vuelvan a entrar, pero NO los saca del historial.
# Ver docs/RUMBO.md §0.1.
/*.csv

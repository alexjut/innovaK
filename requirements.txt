# =======================
# innovaK — dependencias declaradas (high-level)
# Para reproducibilidad estricta usar requirements-lock.txt (freeze completo, 85 paquetes).
# Última actualización: 2026-04-27 con Python 3.10.20.
# =======================

# =======================
# Core Django & Server
# =======================
Django==4.2.11
gunicorn==21.2.0
whitenoise==6.6.0
dj-database-url==2.1.0
python-dotenv==1.1.0

# =======================
# PostgreSQL
# =======================
psycopg2-binary==2.9.10

# =======================
# Panel Admin Mejorado
# =======================
django-jazzmin==2.6.0
django-extensions==3.2.3
django-bootstrap4==23.3

# =======================
# Dashboards (stack moderno)
# =======================
dash>=3.2,<3.3
dash-bootstrap-components>=2.0.1,<2.1  # dbc 2.x requiere Dash 3.x
django-plotly-dash>=2.5.0,<2.6          # habilita Dash 3+
plotly>=5.21,<7                         # seguro con Dash 3 y evita saltos mayores
pandas>=2.2.2

# =======================
# Dash + Flask stack estable y futuro
# =======================
Flask>=3.1,<3.2
Werkzeug>=3.1,<3.2

# =======================
# Geolocalización
# =======================
folium==0.15.1

# =======================
# IA - OpenAI
# =======================
openai==1.10.0

# =======================
# Subida de archivos y API externas
# =======================
requests>=2.31.0
python-magic>=0.4.27

# =======================
# Exportación de datos
# =======================
openpyxl>=3.1.2      # Para exportar a Excel
reportlab>=4.0.7     # Para generar PDFs

# =======================
# Select2 y formularios mejorados
# =======================
django-select2>=8.1.2
django-widget-tweaks>=1.4.12

# =======================
# API REST — Etapa B Plan Frontend
# Convierte endpoints JsonResponse en serializers reusables, listos para
# consumo Angular cuando llegue (regla de oro Angular-ready).
# =======================
djangorestframework==3.15.2
# JWT para clientes externos (Angular, móvil) — coexiste con SessionAuth.
djangorestframework-simplejwt==5.3.1

# =======================
# WebSockets (si usas notificaciones o canales)
# =======================
channels==4.0.0

# =======================
# Cache Redis (Django 4.2 RedisCache nativo)
# =======================
redis==5.3.1  # pin exacto para gov.net (build determinístico)

# Manipulación de PDF y documentos
PyPDF2==3.0.1
python-docx==1.1.0
weasyprint==53.3

# Cifrado AES-256-GCM para documentos en reposo (Mongo)
cryptography>=42,<48

# =======================
# Generación de QR
# =======================
qrcode[pil]==8.2



#mongo
pymongo==4.6.3